<!DOCTYPE html>
<html>
<head>
  <title>Pinteresting</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=devide-width, initial-scale=1.0">
  <!--the above helps for mobile devices, makes it look better-->
</head>
<body>
	<%=render 'layouts/header' %>
	<div class="container">
  <% flash.each do |name, msg| %>
  <%= content_tag(:div, msg, class: "alert alert-#{name}") %>
  <% end %>
  		<%= yield %>
	</div>
  <div class="container botones">
      <div class="row ">
        <div class="btn-group">
          <button class="next btn btn-warning"> PREV </button>
          <button class="reset btn btn-info"> RESET </button>
          <button class="prev btn btn-success"> NEXT </button>
        </div>
      </div>
    </div>
	


</body>
</html>
